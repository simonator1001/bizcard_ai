export interface Plan {
  name: string
  monthlyPrice: number
  yearlyPrice: number
  features: string[]
  popular?: boolean
}

export interface SubscriptionPageProps {
  // Add any props if needed
} 